# 👥 Как работает бот в групповых чатах

## 🎯 Основной принцип:

**ВСЕ участники одной группы видят ОДИНАКОВЫЕ данные!**

## 📊 Изоляция данных:

### ✅ По группам (chatId):
- Каждая группа имеет свой отдельный баланс
- Каждая группа имеет свои транзакции
- Каждая группа имеет свою статистику

### ❌ НЕ по пользователям (userId):
- Данные НЕ разделены между пользователями
- Все участники группы работают с общим балансом
- Личные данные каждого пользователя НЕ отслеживаются

## 💡 Примеры:

### Пример 1: Одна группа
```
Группа "Финансы отдела продаж"
├─ Пользователь Иван добавляет: +50,000 руб
├─ Пользователь Мария добавляет: -10,000 руб
└─ Пользователь Петр вызывает /statistics

✅ Все трое видят баланс: 40,000 руб
✅ Все трое видят все транзакции
```

### Пример 2: Разные группы
```
Группа "Отдел продаж":
  Баланс: 40,000 руб

Группа "Отдел маркетинга":
  Баланс: 15,000 руб

❌ "Отдел продаж" НЕ видит данные "Отдела маркетинга"
❌ "Отдел маркетинга" НЕ видит данные "Отдела продаж"
```

## 🔔 Уведомления:

Когда кто-то добавляет транзакцию:
- ✅ Уведомление отправляется в чат (группу)
- ✅ **ВСЕ участники группы видят уведомление**
- ✅ Это работает в реальном времени

## 💻 Технические детали:

Все операции с базой данных используют `chatId`:

```typescript
// Баланс группы
db.select().from(bankBalance).where(eq(bankBalance.chatId, chatId))

// Транзакции группы
db.select().from(transactions).where(eq(transactions.chatId, chatId))

// Уведомления в группу
sendMessage(chatId, message)  // Все участники видят!
```

## 🎮 Как использовать:

1. **Добавьте бота в групповой чат Telegram**
2. **Дайте боту права администратора** (для отправки сообщений)
3. **Любой участник может:**
   - Добавлять доходы: `+50000 оплата клиента`
   - Добавлять расходы: `-10000 аренда`
   - Проверять баланс: `/bank`
   - Смотреть статистику: `/statistics`
4. **Все участники видят:**
   - Один и тот же баланс
   - Одну и ту же статистику
   - Все транзакции

## ⚠️ Важно:

- Бот НЕ отслеживает, кто именно добавил транзакцию (только имя для уведомлений)
- Все участники группы имеют равные права
- Для разделения данных используйте разные группы в Telegram
