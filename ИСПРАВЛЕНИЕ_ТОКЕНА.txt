================================================================================
ИСПРАВЛЕНИЕ ПРОБЛЕМЫ С TELEGRAM_BOT_TOKEN
================================================================================

Проблема: Бот не загружал файл .env, поэтому не видел токен.

Решение: Код исправлен. Теперь нужно обновить бота на сервере.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ШАГ 1: ЗАПУШЬТЕ ИЗМЕНЕНИЯ В GITHUB
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Выполните на вашем компьютере (где у вас репозиторий):

git add .
git commit -m "Исправлена загрузка .env файла в bot-polling.ts"
git push


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ШАГ 2: ОБНОВИТЕ КОД НА СЕРВЕРЕ TIMEWEB
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Подключитесь к серверу и выполните:

ssh root@ВАШ_IP

cd /root/USTANOVIT
git pull
sudo bash install-simple.sh


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
АЛЬТЕРНАТИВА: ИСПРАВИТЬ ВРУЧНУЮ НА СЕРВЕРЕ (БЫСТРЕЕ)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Если не хотите пушить в GitHub, исправьте прямо на сервере:


1️⃣ Подключитесь к серверу:

ssh root@ВАШ_IP


2️⃣ Проверьте, есть ли файл .env в /root/finance-bot/:

cat /root/finance-bot/.env

   Если файла НЕТ или токен ПУСТОЙ, создайте его:

nano /root/finance-bot/.env

   Добавьте:

TELEGRAM_BOT_TOKEN=ВАШ_ТОКЕН_ОТ_BOTFATHER
NODE_ENV=production
NODE_OPTIONS=--max-old-space-size=1024

   Сохраните: Ctrl+O, Enter, Ctrl+X


3️⃣ Исправьте файл bot-polling.ts (добавьте загрузку dotenv):

nano /root/finance-bot/src/bot-polling.ts

   В самом начале файла (строка 2) должно быть:

import 'dotenv/config';

   Если его нет, добавьте после первой строки:

#!/usr/bin/env node
import 'dotenv/config';
import TelegramBot from 'node-telegram-bot-api';
...

   Сохраните: Ctrl+O, Enter, Ctrl+X


4️⃣ Перезапустите бота:

systemctl restart finance-bot


5️⃣ Проверьте статус:

systemctl status finance-bot

   Или смотрите логи в реальном времени:

journalctl -u finance-bot -f


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ЭКСПРЕСС-КОМАНДЫ (ВСЁ ОДНОЙ СТРОКОЙ)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ Замените YOUR_TOKEN_HERE на реальный токен от @BotFather!

cat > /root/finance-bot/.env << 'EOF'
TELEGRAM_BOT_TOKEN=YOUR_TOKEN_HERE
NODE_ENV=production
NODE_OPTIONS=--max-old-space-size=1024
EOF

systemctl restart finance-bot && sleep 2 && systemctl status finance-bot


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ПРОВЕРКА РАБОТЫ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

После перезапуска в логах должно появиться:

✅ 🚀 Запуск финансового бота в режиме polling...
✅ 📡 Ожидание сообщений от Telegram...
✅ ✅ Финансовый бот запущен и готов к работе!

Если всё ОК - откройте Telegram и напишите боту /start


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ЕСЛИ ОШИБКА "ETELEGRAM: 409"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Эта ошибка значит, что другой экземпляр бота уже запущен.

Решение:

# Остановите все процессы бота
systemctl stop finance-bot
pkill -f "bot-polling"
sleep 5

# Запустите снова
systemctl start finance-bot
journalctl -u finance-bot -f


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ПОЛЕЗНЫЕ КОМАНДЫ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Просмотр .env:
    cat /root/finance-bot/.env

Редактирование .env:
    nano /root/finance-bot/.env

Перезапуск бота:
    systemctl restart finance-bot

Логи (последние 50 строк):
    journalctl -u finance-bot -n 50

Логи (в реальном времени):
    journalctl -u finance-bot -f

Статус бота:
    systemctl status finance-bot

Остановить бота:
    systemctl stop finance-bot


🚀 Готово! Теперь бот должен запуститься без проблем!
