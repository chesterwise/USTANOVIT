# 🤖 Финансовый Telegram Бот

Бот для учёта финансов через Telegram. Работает **БЕЗ Cloudflare** и **БЕЗ внешних API** (OpenAI, ChatGPT и т.д.).

## ✨ Возможности

- 💰 Управление банком (установка и просмотр баланса)
- 📈 Учёт прибыли (доходов)
- 📉 Учёт расходов
- 👥 Личные расходы сотрудников (ZY, AO, MIO)
- 🔄 Учёт споров
- 💳 Учёт долгов
- 📌 Учёт висяков
- 📊 Подробная статистика по всем операциям

## 🚀 Быстрая установка на TimeWeb

### Шаг 1: Подключитесь к серверу

```bash
ssh root@91.186.196.97
```

### Шаг 2: Скачайте проект

```bash
cd /root
# Если у вас есть Git репозиторий:
git clone <ваш-репозиторий> finance-bot

# Или скопируйте файлы проекта любым способом
```

### Шаг 3: Запустите установку

```bash
cd finance-bot
sudo bash install-simple.sh
```

Скрипт автоматически:
- ✅ Установит Node.js 20
- ✅ Установит все зависимости
- ✅ Создаст базу данных
- ✅ Настроит автозапуск бота
- ✅ Запустит бота

### Шаг 4: Готово!

После установки бот будет запущен и готов к работе. Откройте Telegram и напишите вашему боту `/start`.

---

## 📝 Получение токена Telegram бота

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/botfather)
3. Отправьте команду `/newbot`
4. Следуйте инструкциям:
   - Введите имя бота (например: "My Finance Bot")
   - Введите username бота (должен заканчиваться на `bot`, например: `my_finance_bot`)
5. Скопируйте токен, который выдаст BotFather
6. Вставьте его при установке или в файл `.env`

---

## 🎮 Управление ботом

После установки используйте скрипт `bot-manager.sh` для удобного управления:

```bash
# Показать все команды
./bot-manager.sh help

# Запустить бота
./bot-manager.sh start

# Остановить бота
./bot-manager.sh stop

# Перезапустить бота
./bot-manager.sh restart

# Показать статус
./bot-manager.sh status

# Посмотреть логи (в реальном времени)
./bot-manager.sh logs

# Посмотреть последние 100 строк логов
./bot-manager.sh logs-tail

# Редактировать настройки (.env)
./bot-manager.sh edit-env
```

### Или используйте systemctl напрямую:

```bash
# Перезапуск
systemctl restart finance-bot

# Остановка
systemctl stop finance-bot

# Запуск
systemctl start finance-bot

# Статус
systemctl status finance-bot

# Логи
journalctl -u finance-bot -f
```

---

## 📋 Команды бота

### 💰 Управление банком
- `/bank` - показать текущий баланс
- `/bank 1000000` - установить баланс банка

### 💵 Операции
- `+50000` - добавить прибыль
- `+50000 оплата клиента` - добавить прибыль с заметкой
- `-10000` - добавить расход
- `-10000 аренда офиса` - добавить расход с заметкой

### 💳 Специальные расходы
- `/debts 5000` - добавить долг
- `/visyak 3000` - добавить висяк
- `/dispute 20000` - закрыть спор

### 👥 Личные расходы сотрудников
- `-Z5000` - вычесть у ZY
- `-Z5000 аванс` - вычесть у ZY с заметкой
- `+Z2000` - добавить к ZY (например, возврат)
- `-A3000` - вычесть у AO
- `+A1000` - добавить к AO
- `-M4000` - вычесть у MIO
- `+M1500` - добавить к MIO

### 📊 Статистика
- `/statistics` - общая статистика
- `/statistics_income` - статистика прибыли
- `/statistics_expense` - статистика расходов
- `/statistics_disputes` - статистика споров
- `/statistics_employees` - расходы сотрудников

### 🔔 Уведомления
- `/notify_on` - включить уведомления
- `/notify_off` - выключить уведомления

### ℹ️ Помощь
- `/start` или `/menu` - главное меню
- `/help` - справка по командам

---

## ⚙️ Настройка

### Файл .env

Все настройки находятся в файле `/root/finance-bot/.env`:

```bash
# Обязательно!
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather

# Опционально
NODE_ENV=production
NODE_OPTIONS=--max-old-space-size=1024
```

После изменения `.env` перезапустите бота:
```bash
./bot-manager.sh restart
# или
systemctl restart finance-bot
```

---

## 🗄️ База данных

Бот использует SQLite базу данных (файл `finance.db`).

Все данные хранятся локально на сервере:
- ✅ Транзакции (доходы, расходы)
- ✅ Баланс банка
- ✅ Состояния пользователей
- ✅ Настройки уведомлений

---

## 🔧 Технические особенности

### Режим работы: Long Polling

Бот работает в режиме **long polling** (постоянного опроса Telegram API):
- ✅ **НЕ требует** публичного URL
- ✅ **НЕ требует** Cloudflare Tunnel
- ✅ **НЕ требует** настройки webhook
- ✅ **НЕ требует** SSL сертификата
- ✅ Работает из-под любого IP адреса
- ✅ Работает даже за NAT

### Без внешних API

Бот работает **полностью автономно**:
- ❌ **НЕ использует** OpenAI / ChatGPT
- ❌ **НЕ использует** другие LLM API
- ✅ Использует собственный парсер команд
- ✅ Все команды обрабатываются локально

---

## 📊 Мониторинг

### Просмотр логов в реальном времени

```bash
journalctl -u finance-bot -f
```

### Последние 100 строк логов

```bash
journalctl -u finance-bot -n 100
```

### Проверка статуса

```bash
systemctl status finance-bot
```

Или используйте:
```bash
./bot-manager.sh status
```

---

## 🆘 Решение проблем

### Бот не запускается

1. Проверьте логи:
```bash
./bot-manager.sh logs-tail
```

2. Проверьте, установлен ли токен бота:
```bash
cat /root/finance-bot/.env | grep TELEGRAM_BOT_TOKEN
```

3. Проверьте, что токен правильный (через BotFather)

### Бот не отвечает на команды

1. Проверьте, что бот запущен:
```bash
./bot-manager.sh status
```

2. Убедитесь, что вы написали `/start` боту в Telegram

3. Проверьте логи на наличие ошибок:
```bash
./bot-manager.sh logs
```

### Ошибка "409 Conflict"

Это означает, что другой экземпляр бота уже запущен.

Решение:
```bash
./bot-manager.sh stop
sleep 5
./bot-manager.sh start
```

### База данных повреждена

Создайте резервную копию и переинициализируйте:
```bash
cd /root/finance-bot
cp finance.db finance.db.backup
npx tsx scripts/create-tables-safe.ts
./bot-manager.sh restart
```

---

## 🔄 Обновление бота

1. Остановите бота:
```bash
./bot-manager.sh stop
```

2. Обновите код (Git или копирование файлов):
```bash
cd /root/finance-bot
git pull
# или скопируйте новые файлы
```

3. Установите новые зависимости (если есть):
```bash
npm install
```

4. Запустите бота:
```bash
./bot-manager.sh start
```

---

## 🗑️ Удаление бота

### Через скрипт управления:

```bash
./bot-manager.sh uninstall
```

### Вручную:

```bash
# Остановка и удаление сервиса
systemctl stop finance-bot
systemctl disable finance-bot
rm /etc/systemd/system/finance-bot.service
systemctl daemon-reload

# Удаление файлов проекта (опционально)
rm -rf /root/finance-bot
```

---

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте логи: `./bot-manager.sh logs-tail`
2. Проверьте статус: `./bot-manager.sh status`
3. Попробуйте перезапустить: `./bot-manager.sh restart`

---

## 📄 Лицензия

Этот проект создан для личного использования.

---

**Удачи! 🚀**
