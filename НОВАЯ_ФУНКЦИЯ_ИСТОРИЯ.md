# ✅ Добавлена История Операций за 24 часа

## Что нового:

### 📅 Команда `/history`
Теперь вы можете посмотреть все транзакции за последние 24 часа с помощью команды:
- `/history` 
- `/history_24h`

### 🔘 Кнопка в меню
Добавлена кнопка **"📅 История 24ч"** в меню "📊 Статистика"

## Что показывает история:

1. **Всего операций** за 24 часа
2. Для каждой транзакции:
   - ✅ Тип операции (Прибыль, Расход, Личный расход, Спор)
   - 💰 Сумма с знаком (+/-)
   - 👤 Сотрудник (если применимо)
   - 📝 Заметка (если есть)
   - 👨‍💼 Кто внес запись
   - ⏰ Дата и время (в формате МСК)

## Пример вывода:

```
📅 ИСТОРИЯ ЗА ПОСЛЕДНИЕ 24 ЧАСА:

📊 Всего операций: 5

1. 📈 Прибыль
   💰 +50000.00 руб.
   📝 Оплата от клиента
   👨‍💼 Внес: Иван
   ⏰ 10.11.2025, 14:30

2. 📉 Расход
   💰 -10000.00 руб.
   📝 Аренда офиса
   👨‍💼 Внес: Иван
   ⏰ 10.11.2025, 15:00

3. 👤 Личный расход
   💰 -5000.00 руб.
   👤 Сотрудник: ZY
   📝 Аванс
   👨‍💼 Внес: Иван
   ⏰ 10.11.2025, 16:45

...
```

## Как использовать:

### Способ 1: Через команду
Просто напишите боту:
```
/history
```

### Способ 2: Через кнопки
1. Откройте главное меню: `/start` или `/menu`
2. Нажмите **"📊 Статистика"**
3. Нажмите **"📅 История 24ч"**

## Как обновить бота на сервере:

### Вариант 1: Обновление с GitHub (рекомендуется)

```bash
# 1. На вашем компьютере (где репозиторий):
git add .
git commit -m "Добавлена история операций за 24 часа"
git push

# 2. На сервере Timeweb:
ssh root@ВАШ_IP

cd /root/USTANOVIT
git pull

# Копируем обновленные файлы
cp -r /root/USTANOVIT/* /root/finance-bot/

# Перезапускаем бота
systemctl restart finance-bot

# Проверяем статус
journalctl -u finance-bot -f
```

### Вариант 2: Ручное обновление файлов

Если не хотите использовать git, можете просто скопировать измененные файлы на сервер:

**Измененные файлы:**
- `src/mastra/utils/commandParser.ts`
- `src/mastra/tools/financeTool.ts`
- `src/mastra/utils/telegramButtons.ts`
- `src/bot-polling.ts`

```bash
# После загрузки файлов на сервер:
systemctl restart finance-bot
journalctl -u finance-bot -f
```

## Технические детали:

- ⏰ История показывает транзакции за **ровно 24 часа** назад от текущего момента
- 📊 Сортировка: от **новых к старым**
- 💾 Данные берутся из таблицы `transactions` в базе данных
- 🕐 Время отображается в формате **МСК (UTC+3)**

## Проверка работы:

После обновления попробуйте:

1. Создайте несколько тестовых транзакций:
   ```
   +1000 тест прибыль
   -500 тест расход
   -Z200 тест личный
   ```

2. Посмотрите историю:
   ```
   /history
   ```

3. Или через кнопки:
   - Главное меню → Статистика → История 24ч

Готово! 🚀
