# 🚀 Автоматическая установка Finance Bot на Timeweb

Это полностью автоматический скрипт для развертывания Telegram бота на сервере Timeweb с использованием Cloudflare Tunnel.

## ✨ Что делает скрипт автоматически?

1. ✅ Устанавливает Node.js 20
2. ✅ Устанавливает все необходимые утилиты
3. ✅ Устанавливает и настраивает Cloudflare Tunnel
4. ✅ Копирует/клонирует проект
5. ✅ Настраивает переменные окружения
6. ✅ Устанавливает все npm зависимости
7. ✅ Собирает проект
8. ✅ Настраивает PM2 для автозапуска
9. ✅ Запускает все службы
10. ✅ Создает публичный URL через Cloudflare Tunnel

## 📋 Требования

- Сервер Ubuntu 22.04 на Timeweb
- Доступ по SSH с правами root
- Telegram Bot Token (получить у @BotFather)
- OpenAI API Key (опционально, если используется AI)

## 🎯 Быстрый старт (для новой установки)

### Вариант 1: Установка прямо из текущей директории

Если вы уже скачали проект на сервер:

```bash
# Подключитесь к серверу
ssh root@ваш_ip

# Перейдите в директорию с проектом
cd /путь/к/проекту

# Сделайте скрипт исполняемым
chmod +x install-timeweb.sh

# Запустите установку
./install-timeweb.sh
```

### Вариант 2: Установка из Git репозитория

```bash
# Подключитесь к серверу
ssh root@ваш_ip

# Скачайте только скрипт установки
wget https://raw.githubusercontent.com/your-repo/your-bot/main/install-timeweb.sh

# Сделайте скрипт исполняемым
chmod +x install-timeweb.sh

# Запустите установку (скрипт сам предложит ввести URL репозитория)
./install-timeweb.sh
```

### Вариант 3: Одной командой (самый быстрый способ)

```bash
# Скачайте проект напрямую и запустите установку
cd /root
git clone https://github.com/your-username/your-repo.git finance-bot
cd finance-bot
chmod +x install-timeweb.sh
./install-timeweb.sh
```

## ⚙️ Что нужно подготовить заранее

1. **Telegram Bot Token**
   - Найдите @BotFather в Telegram
   - Отправьте команду `/newbot`
   - Следуйте инструкциям
   - Скопируйте полученный токен

2. **OpenAI API Key** (если используете AI функции)
   - Зарегистрируйтесь на https://platform.openai.com
   - Перейдите в API Keys
   - Создайте новый ключ

## 📝 Процесс установки

Скрипт автоматически:

1. Обновит систему
2. Установит Node.js 20
3. Установит Cloudflare Tunnel
4. Спросит у вас:
   - URL Git репозитория (если проект не найден)
   - Telegram Bot Token
   - OpenAI API Key (опционально)
5. Установит все зависимости
6. Соберет проект
7. Запустит PM2
8. Запустит Cloudflare Tunnel
9. Покажет вам публичный URL

## 🔗 Настройка Telegram Webhook

После установки нужно настроить webhook для Telegram:

```bash
# Сделайте скрипт исполняемым
chmod +x setup-telegram-webhook.sh

# Запустите настройку webhook
./setup-telegram-webhook.sh
```

Скрипт автоматически:
- Найдет URL вашего Cloudflare Tunnel
- Настроит webhook в Telegram
- Проверит статус webhook

## 🎮 Управление ботом

Используйте интерактивное меню для управления:

```bash
# Сделайте скрипт исполняемым
chmod +x manage-bot.sh

# Запустите меню управления
./manage-bot.sh
```

Меню позволяет:
- 📊 Показать статус всех служб
- 📝 Просмотреть логи
- 🔄 Перезапустить бота
- 🛑 Остановить/запустить бота
- 🔗 Настроить webhook
- 🔗 Показать URL туннеля
- 📥 Обновить бота из Git
- ⚙️ Редактировать .env

## 📊 Полезные команды

### PM2 (управление процессами)

```bash
pm2 list                  # Список всех процессов
pm2 logs                  # Показать все логи
pm2 logs finance-bot      # Логи основного бота
pm2 logs inngest-server   # Логи inngest сервера
pm2 restart all           # Перезапустить всё
pm2 stop all              # Остановить всё
pm2 start all             # Запустить всё
```

### Cloudflare Tunnel

```bash
# Статус туннеля
systemctl status cloudflare-tunnel

# Логи туннеля
journalctl -u cloudflare-tunnel -f

# Получить URL туннеля
journalctl -u cloudflare-tunnel | grep trycloudflare.com

# Перезапустить туннель
systemctl restart cloudflare-tunnel
```

### Общие команды

```bash
# Редактировать переменные окружения
nano /root/finance-bot/.env

# Проверить, работает ли сервер
curl http://localhost:3001/health

# Проверить логи системы
journalctl -xe
```

## 🔄 Обновление бота

### Автоматическое обновление

```bash
./manage-bot.sh
# Выберите опцию 8) Обновить бота из Git
```

### Ручное обновление

```bash
cd /root/finance-bot
git pull
npm install
npm run build
pm2 restart all
./setup-telegram-webhook.sh  # Если изменился код webhook
```

## 🐛 Решение проблем

### Бот не отвечает

```bash
# Проверьте статус
pm2 list

# Проверьте логи на ошибки
pm2 logs finance-bot --lines 100

# Проверьте webhook
curl "https://api.telegram.org/bot<ВАШ_ТОКЕН>/getWebhookInfo"
```

### Cloudflare Tunnel не работает

```bash
# Проверьте статус
systemctl status cloudflare-tunnel

# Посмотрите логи
journalctl -u cloudflare-tunnel -n 50

# Перезапустите туннель
systemctl restart cloudflare-tunnel
```

### PM2 не запускается при перезагрузке

```bash
# Переустановите автозапуск
pm2 startup systemd -u root --hp /root
pm2 save
```

### Проблемы с портами

```bash
# Проверьте, что слушается на портах
netstat -tulpn | grep -E ':(3000|3001|5000)'

# Или используйте ss
ss -tulpn | grep -E ':(3000|3001|5000)'
```

## 📂 Структура проекта

```
/root/finance-bot/
├── src/                      # Исходный код
│   ├── mastra/              # Mastra компоненты
│   │   ├── agents/          # AI агенты
│   │   ├── tools/           # Инструменты
│   │   ├── workflows/       # Рабочие процессы
│   │   └── index.ts         # Главный файл Mastra
│   └── triggers/            # Триггеры (Telegram, etc)
├── scripts/                 # Скрипты
│   └── inngest.sh          # Запуск Inngest
├── logs/                    # Логи приложения
├── .env                     # Переменные окружения
├── package.json             # npm зависимости
├── ecosystem.config.cjs     # PM2 конфигурация
├── install-timeweb.sh       # 🚀 Скрипт автоустановки
├── setup-telegram-webhook.sh # 🔗 Настройка webhook
└── manage-bot.sh            # 🎮 Меню управления
```

## 🔒 Безопасность

1. **Никогда не публикуйте .env файл** - он содержит секретные ключи
2. **Регулярно обновляйте систему**: `apt update && apt upgrade`
3. **Используйте SSH ключи** вместо паролей
4. **Настройте firewall**: `ufw enable && ufw allow 22`
5. **Регулярно делайте бэкапы**: `/root/finance-bot`

## 🌐 Cloudflare Tunnel

Скрипт автоматически создает **бесплатный публичный URL** через Cloudflare Tunnel.

**Преимущества:**
- ✅ Бесплатно
- ✅ Без настройки DNS
- ✅ HTTPS из коробки
- ✅ Автоматическое обновление сертификатов
- ✅ Работает за NAT и файрволами

**URL будет выглядеть так:**
```
https://abc-def-123.trycloudflare.com
```

⚠️ **Важно:** URL может измениться при перезапуске туннеля. После перезапуска туннеля нужно заново настроить webhook:

```bash
./setup-telegram-webhook.sh
```

## ❓ FAQ

**Q: Можно ли использовать свой домен вместо trycloudflare.com?**
A: Да, для этого нужно зарегистрироваться в Cloudflare и создать именованный туннель. Это требует дополнительной настройки.

**Q: Бот не отвечает в Telegram, что делать?**
A: 
1. Проверьте логи: `pm2 logs finance-bot`
2. Проверьте webhook: `./setup-telegram-webhook.sh`
3. Убедитесь, что Cloudflare Tunnel работает: `systemctl status cloudflare-tunnel`

**Q: Как сменить порт приложения?**
A: Измените PORT в .env файле и перезапустите: `nano .env` → `pm2 restart all`

**Q: Сколько стоит Cloudflare Tunnel?**
A: Бесплатно! Но URL будет временным. Для постоянного URL нужен платный аккаунт Cloudflare.

**Q: Можно ли запустить несколько ботов на одном сервере?**
A: Да, но нужно изменить порты и создать отдельные PM2 процессы для каждого.

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `./manage-bot.sh` → опция 2
2. Убедитесь, что все службы запущены: `./manage-bot.sh` → опция 1
3. Попробуйте перезапустить: `./manage-bot.sh` → опция 3

## 🎉 Готово!

После успешной установки ваш бот:
- ✅ Запущен и работает 24/7
- ✅ Автоматически перезапускается при сбоях
- ✅ Доступен через публичный HTTPS URL
- ✅ Готов принимать сообщения в Telegram

Отправьте сообщение вашему боту в Telegram для проверки! 🚀
